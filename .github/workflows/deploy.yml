name: Deploy to Render

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render Deploy
        run: |
          curl -X POST \
            "https://api.render.com/deploy/srv-d8aajurbc2fs73fsh5lg?key=${{ secrets.RENDER_DEPLOY_KEY }}" \
            -H "Accept: application/json" \
            -H "Content-Type: application/json" \
            --data '{}'
