name: Deploy to Render

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render Deploy
        run: |
          curl -X POST \
            "https://api.render.com/deploy/srv-d8aajurbc2fs73fsh5lg?key=mwl-slHBmsQ" \
            -H "Accept: application/json" \
            -H "Content-Type: application/json" \
            --data '{}'
